check_valid_extension:
    This function checks if the file's extension is ".cub"
    if it is the file is valid otherwise it's an error.

read_file_and_get_informations:
    This function iterates through the file line by line then it searches
    for any printable character, the first line that has a printable character
    is the first element, then this step will be repeated until we collect all
    the 6 elements we need.

check_if_informations_are_valid:
    In this function we created a linked list that contains all the 6 elements
    and send it to other function to check if they're ordered correctly.

